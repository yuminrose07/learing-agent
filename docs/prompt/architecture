# Architecture Boundaries

## 目标

- 本文档定义项目的架构边界，用于说明哪些能力属于 Core，哪些能力属于 Extensions。
- 它不是日常默认加载的核心规则，而是在需要判断职责归属、改动边界和 Hook 行为时按需加载。

## Core 与 Extensions

- `Core`：会话生命周期、状态隔离、协议合法性、持久化、核心恢复逻辑。
- `Extensions`：工具、Hook、观测、校验、审计、压缩等可插拔能力。

## 判断标准

- 如果某个能力缺失后会让 Agent 无法继续运行，或者破坏会话正确性、协议合法性、状态一致性，它属于 `Core`。
- 如果某个能力缺失后不会导致核心运行失效，只会影响策略、观测、约束收紧或体验优化，它属于 `Extensions`。
- 是否允许高频试验、灰度调整、独立失效后隔离，也是判断是否应放入 `Extensions` 的重要信号。

## Hook 规则

- Hook 默认可以修改数据、补充上下文、记录告警。
- Hook 默认不应中断核心流程。
- 只有权限拒绝、安全校验失败、严格模式等明确场景，才允许 `abort`。
- Hook 设计应以“增强核心流程”为目标，而不是替代核心流程。

## 使用建议

- 当任务涉及职责划分、模块归属、边界争议时，先加载本文件再做判断。
- 当任务只是普通编码、局部修复、非边界型调整时，不需要默认加载本文件。
- 如果未来架构边界发生变化，应先更新本文件，再检查相关扩展模块是否仍然一致。
